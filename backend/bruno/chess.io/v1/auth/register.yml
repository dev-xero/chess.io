info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE}}/api/v1/auth/register"
  body:
    type: form-urlencoded
    data:
      - name: username
        value: tester
      - name: password
        value: "1"
      - name: secret_question
        value: what glows bright red
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
